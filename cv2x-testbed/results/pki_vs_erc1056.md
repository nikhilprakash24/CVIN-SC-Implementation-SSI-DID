# PKI vs ERC-1056 identity: identical operation set, measured

Generated by `scripts/experiment_pki_vs_erc1056.py`. Every backend runs the same seven operations with the same inputs and the same result checks; each figure is over n=50 warm repetitions after 3 discarded warm-up runs, timed with `time.perf_counter_ns`. Gas is the exact `gasUsed` from the transaction receipt.

## Environment

| Item | Value |
|---|---|
| date_utc | 2026-09-24T22:18:09+00:00 |
| git_commit | ed85dfd |
| git_dirty | True |
| python_version | 3.11.15 |
| node_version | v22.22.2 |
| hardhat_version | 2.27.0 |
| ethers_version | 6.15.0 |
| web3_version | 8.0.0 |
| solc_version | 0.8.20+commit.a1b79de6 |
| cryptography_version | 41.0.7 |
| chain_id | 1337 |
| block_gas_limit | 30000000 |
| automine | True |
| contract_address | 0x5FbDB2315678afecb367f032d93F642f64180aa3 |
| contract_deploy_gas | 878509 |
| cpu_model | Intel(R) Xeon(R) Processor @ 2.10GHz |
| cpu_count | 4 |
| os | Linux-6.18.44-fc-v37-x86_64-with-glibc2.39 |

## Results (milliseconds; gas in units)

| Backend | Operation | Chain round trip | n | median | p95 | mean | min | max | gas (receipt) | RPC calls/op |
|---|---|:---:|---:|---:|---:|---:|---:|---:|---:|---:|
| pki_standard | register_identity | no | 50 | 15.167 | 19.528 | 15.854 | 14.812 | 26.309 | - | - |
| pki_standard | issue_credential | no | 50 | 0.721 | 0.878 | 0.760 | 0.663 | 1.613 | - | - |
| pki_standard | sign_message | no | 50 | 0.054 | 0.089 | 0.059 | 0.050 | 0.118 | - | - |
| pki_standard | verify_message | no | 50 | 0.321 | 0.366 | 0.324 | 0.294 | 0.375 | - | - |
| pki_standard | resolve_identity | no | 50 | 0.171 | 0.196 | 0.175 | 0.160 | 0.217 | - | - |
| pki_standard | check_revocation | no | 50 | 0.001 | 0.001 | 0.001 | 0.001 | 0.003 | - | - |
| pki_standard | revoke_credential | no | 50 | 0.020 | 0.044 | 0.023 | 0.019 | 0.050 | - | - |
| pki_centralized | register_identity | no | 50 | 5.023 | 5.600 | 5.116 | 4.646 | 6.788 | - | - |
| pki_centralized | issue_credential | no | 50 | 0.227 | 0.289 | 0.234 | 0.193 | 0.317 | - | - |
| pki_centralized | sign_message | no | 50 | 0.058 | 0.082 | 0.061 | 0.050 | 0.100 | - | - |
| pki_centralized | verify_message | no | 50 | 0.369 | 0.418 | 0.376 | 0.338 | 0.427 | - | - |
| pki_centralized | resolve_identity | no | 50 | 0.018 | 0.029 | 0.020 | 0.017 | 0.042 | - | - |
| pki_centralized | check_revocation | no | 50 | 0.000 | 0.001 | 0.001 | 0.000 | 0.002 | - | - |
| pki_centralized | revoke_credential | no | 50 | 0.018 | 0.036 | 0.020 | 0.015 | 0.050 | - | - |
| erc1056_did | register_identity | yes | 50 | 16.912 | 22.034 | 18.266 | 14.505 | 63.921 | 54860 (54836..54860) | 6 |
| erc1056_did | issue_credential | yes | 50 | 13.517 | 15.697 | 13.573 | 11.160 | 17.916 | 37779 (37755..37779) | 5 |
| erc1056_did | sign_message | no | 50 | 0.401 | 0.456 | 0.407 | 0.382 | 0.484 | - | - |
| erc1056_did | verify_message | yes | 50 | 18.158 | 23.741 | 18.667 | 15.950 | 25.493 | - | 7 |
| erc1056_did | resolve_identity | yes | 50 | 10.058 | 12.753 | 10.816 | 8.575 | 30.206 | - | 4 |
| erc1056_did | check_revocation | yes | 50 | 7.712 | 9.365 | 7.801 | 6.450 | 11.322 | - | 3 |
| erc1056_did | revoke_credential | yes | 50 | 12.568 | 15.534 | 13.006 | 10.979 | 19.432 | 75044 (75032..75044) | 8 |

### Side-by-side (median / p95, ms)

| Operation | pki_standard | pki_centralized | erc1056_did |
|---|---|---|---|
| register_identity | 15.167 / 19.528 | 5.023 / 5.600 | 16.912 / 22.034 |
| issue_credential | 0.721 / 0.878 | 0.227 / 0.289 | 13.517 / 15.697 |
| sign_message | 0.054 / 0.089 | 0.058 / 0.082 | 0.401 / 0.456 |
| verify_message | 0.321 / 0.366 | 0.369 / 0.418 | 18.158 / 23.741 |
| resolve_identity | 0.171 / 0.196 | 0.018 / 0.029 | 10.058 / 12.753 |
| check_revocation | 0.001 / 0.001 | 0.000 / 0.001 | 7.712 / 9.365 |
| revoke_credential | 0.020 / 0.044 | 0.018 / 0.036 | 12.568 / 15.534 |

Signed-message size (JSON bytes, median): pki_standard=1078, pki_centralized=1106, erc1056_did=595.

JSON-RPC methods issued per ERC-1056 operation (first warm run):

- `register_identity`: eth_getTransactionCount, eth_gasPrice, eth_chainId, eth_sendRawTransaction, eth_getTransactionReceipt, eth_chainId
- `issue_credential`: eth_getTransactionCount, eth_gasPrice, eth_chainId, eth_sendRawTransaction, eth_getTransactionReceipt
- `verify_message`: eth_chainId, eth_call, eth_chainId, eth_getLogs, eth_chainId, eth_call, eth_chainId
- `resolve_identity`: eth_chainId, eth_call, eth_chainId, eth_getLogs
- `check_revocation`: eth_chainId, eth_call, eth_chainId
- `revoke_credential`: eth_getTransactionCount, eth_gasPrice, eth_chainId, eth_sendRawTransaction, eth_getTransactionReceipt, eth_chainId, eth_call, eth_chainId

## Sanity checks (untimed, must all be True)

| Backend | verify after revocation is False | tampered message rejected | genuine message accepted |
|---|:---:|:---:|:---:|
| pki_standard | True | True | True |
| pki_centralized | True | True | True |
| erc1056_did | True | True | True |

## What each operation actually does per backend

| Operation | pki_standard / pki_centralized (X.509, ECDSA P-256) | erc1056_did (ERC1056Registry, ECDSA secp256k1) |
|---|---|---|
| register_identity | generate P-256 key; CA issues 1 enrollment certificate + 20 pseudonym certificates (all in-process) | generate secp256k1 key; vehicle account sends `registerVehicle(addr, pubkey)` (1 tx, wait for receipt) |
| issue_credential | CA issues 1 additional pseudonym certificate (new key, CA-signed) | vehicle sends `updateVehicleKey(addr, newPubkey)` (1 tx; emits DIDAttributeChanged) |
| sign_message | ECDSA-SHA256 over canonical JSON with current pseudonym key; message carries the PEM certificate | ECDSA-SHA256 over canonical JSON with the vehicle key; message carries only the `did:ethr` string |
| verify_message | parse PEM cert, CRL set lookup, validity window, issuer name check, ECDSA verify (no network) | `getIdentityInfo` (eth_call) + event walk (eth_getLogs) to resolve the key + `isRevoked` (eth_call), then ECDSA verify |
| resolve_identity | in-memory lookup of the enrollment certificate / public key | `getIdentityInfo` (eth_call) + `previousChange` event walk (eth_getLogs per hop) |
| check_revocation | in-memory CRL/flag lookup | `isRevoked` (eth_call) |
| revoke_credential | add enrollment + 20 pseudonym serials to the CA's CRL set (in-memory) | vehicle sends `revokeIdentity(addr)` (1 tx) |

## Caveats (read before quoting any number)

1. **What the PKI baseline is.** Both PKI backends are *in-process* Python implementations (`cryptography` X.509/ECDSA P-256): the CA, the CRL and the vehicles live in the same process. There is no CA network round trip for enrollment, no OCSP responder, no CRL download, no certificate-chain building beyond an issuer-name comparison, and the CRL is a Python `set` whose size during this run is at most a few hundred serial numbers (21 per revoked vehicle). Real IEEE 1609.2 / ETSI PKI adds network latency to enrollment, pseudonym provisioning and CRL/OCSP freshness that this baseline does not model. The PKI numbers are therefore a *lower bound* on real PKI cost, not a measurement of a deployed PKI.
2. **Hardhat local latency is not public-network latency.** The ERC-1056 chain is a single Hardhat node on localhost with automine (a transaction is mined the instant it is received). The chain round-trip figures measure web3.py + HTTP JSON-RPC + Hardhat's EVM on this machine; compare `check_revocation` (exactly one `eth_call`) to see the per-round-trip floor of this stack, which is the dominant term in every ERC-1056 read. A production client (Geth/Erigon RPC, keep-alive connection, or an in-process light client) has a different floor. On a public or consortium network, write operations (register, issue, revoke) take one block interval or more (seconds to minutes) plus confirmation depth, and reads depend on the RPC endpoint's latency. Only the *shape* of the comparison (which operations need a chain round trip and how many) transfers; the millisecond values do not.
   The RPC call lists above show that web3.py 8.0.0 issues an `eth_chainId` request around most calls (4 of the 7 round trips in `verify_message`). These are client-side redundancy, not registry work: enabling web3's request caching (`w3.provider.cache_allowed_requests = True`) or pinning the chain id removes them, leaving 3 registry round trips per uncached verification (2 `eth_call` + 1 `eth_getLogs`). The provider is measured as shipped, without that tuning.
3. **Gas is deterministic; latency is not.** `gasUsed` depends only on the contract code, the EVM version and the transaction inputs, so the receipt value is exact and re-running with the same inputs reproduces it; no confidence interval is needed for gas. The small spread the table shows within one operation (a `min..max` range, at most a few tens of gas) is *not* measurement noise: it is EIP-2028 calldata pricing (4 gas per zero byte vs 16 per non-zero byte) applied to the randomly generated public-key / address bytes of each repetition. Latency, by contrast, varies with scheduling, GC, RPC and EVM state, hence n repetitions, the median (robust) and p95 (tail) rather than a single run.
4. **Where the backends are not equivalent.**
   - `register_identity` does far more work on the PKI side (21 certificate issuances, each an ECDSA sign by the CA) than on the ERC-1056 side (one transaction storing one key). ERC-1056 has no pseudonym pool; adding pseudonymity would require one DID per pseudonym (one registration each).
   - `verify_message` on the PKI side is self-contained because the certificate travels with the message; on the ERC-1056 side the message carries only the DID, so the verifier must resolve the key and revocation status from the registry (3 RPC calls per verification in this provider, no caching). A production verifier would cache resolved DID documents and re-validate with a single `changed(identity)` call; that optimisation is not implemented here, so the ERC-1056 verify figure is the *uncached, worst-case* cost.
   - `revoke_credential` on the PKI side revokes every certificate of the vehicle; on the ERC-1056 side one flag revokes the identity. Checking revocation is an in-memory lookup vs. an `eth_call`.
   - `issue_credential` compares a CA-signed pseudonym certificate with an owner-signed on-chain key attribute: different trust anchors (CA vs. identity owner) doing analogous work (bind a new key).
   - The ERC-1056 write operations are signed by the vehicle's own account (`ERC1056Registry` is `onlyOwner`); funding that account with ETH is a provisioning step done before the timer starts and is excluded from the measurements. Signature curves also differ (P-256 for PKI, secp256k1 for ERC-1056), both with SHA-256.
5. **Provider bugs fixed to make the experiment possible** (see the diff): the ERC-1056 provider previously returned a placeholder public key (`"0x04..."`) from resolution, so its `verify_message` could never succeed, and it signed `registerVehicle`/`revokeIdentity` with the deployer account, which the contract rejects (`Only owner can perform this action`). The deploy script's smoke test had the same bug. These fixes affect correctness, not the measured algorithm.
6. **Single machine, single process, no concurrency.** Throughput under load, RPC contention and multi-vehicle broadcast scenarios are out of scope here.
